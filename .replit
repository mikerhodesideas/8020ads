modules = ["nodejs-20"]
run = "node server.js"

[nix]
channel = "stable-24_05"

[[ports]]
localPort = 3333
externalPort = 80

[deployment]
deploymentTarget = "gce"
run = ["node", "server.js"]
build = ["sh", "-c", "npm install --production 2>/dev/null || true"]
